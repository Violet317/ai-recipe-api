# AI食谱推荐系统 - 前端

这是一个基于React的AI食谱推荐系统前端应用，与后端API配合使用，为用户提供食材匹配的食谱推荐功能。

## 技术栈

- **框架**: React 18
- **语言**: TypeScript
- **构建工具**: Vite
- **路由**: React Router DOM
- **HTTP请求**: Axios
- **样式**: CSS
- **部署**: Docker + Nginx + Railway

## 功能特性

- 📝 **食材输入**: 支持输入多种食材（用逗号分隔）
- 🏷️ **标签过滤**: 支持通过标签过滤食谱（如低脂、快手等）
- 📊 **匹配度展示**: 显示每个食谱与输入食材的匹配度
- 📋 **缺少食材**: 列出制作该食谱所需的额外食材
- 🕒 **烹饪时间**: 显示每个食谱的烹饪时间
- 👤 **用户认证**: 支持用户注册和登录
- 📱 **响应式设计**: 适配不同屏幕尺寸

## 安装和运行

### 前置条件

- Node.js 16+ 
- npm 8+
- 后端API服务（可参考项目根目录的后端代码）

### 安装依赖

```bash
npm install
```

### 配置环境变量

复制示例环境变量文件并修改：

```bash
cp .env.example .env
```

在`.env`文件中配置API基础URL：

```env
VITE_API_BASE_URL=http://localhost:8000
```

### 开发模式运行

```bash
npm run dev
```

应用将在`http://localhost:5173`启动。

### 构建生产版本

```bash
npm run build
```

构建后的文件将输出到`dist`目录。

## 部署到Railway

### 方法一：使用Railway CLI（推荐）

1. 安装Railway CLI：
   ```bash
   npm install -g @railway/cli
   ```

2. 登录Railway：
   ```bash
   railway login
   ```

3. 初始化项目：
   ```bash
   railway init
   ```

4. 部署项目：
   ```bash
   railway up
   ```

5. 设置环境变量：
   ```bash
   railway env set VITE_API_BASE_URL "https://your-backend-api.railway.app"
   ```

### 方法二：使用GitHub集成

1. 将项目推送到GitHub仓库
2. 在Railway上创建新项目，选择"Deploy from GitHub repository"
3. 选择你的仓库并点击"Deploy Now"
4. 在项目设置中配置环境变量

## 环境变量

| 变量名 | 描述 | 默认值 |
|-------|------|-------|
| VITE_API_BASE_URL | 后端API的基础URL | http://localhost:8000 |

## 项目结构

```
src/
├── components/          # React组件
│   ├── Auth.tsx        # 用户认证组件
│   └── RecipeRecommendation.tsx  # 食谱推荐组件
├── api.ts              # API服务配置
├── App.tsx             # 应用主组件
├── App.css             # 应用样式
├── main.tsx            # 应用入口
└── index.html          # HTML模板
```

## API接口说明

### 食谱推荐
- **URL**: `/recommend`
- **方法**: POST
- **请求体**: 
  ```json
  {
    "ingredients": ["番茄", "鸡蛋"],
    "tags": ["低脂"]
  }
  ```
- **响应**: 
  ```json
  {
    "user_ingredients": ["番茄", "鸡蛋"],
    "filter_tags": ["低脂"],
    "recommendations": [
      {
        "id": 1,
        "name": "番茄炒蛋",
        "match_rate": 1.0,
        "missing_ingredients": [],
        "time": 10,
        "tags": ["快手", "家常菜"]
      }
    ],
    "total": 1
  }
  ```

### 用户注册
- **URL**: `/register`
- **方法**: POST
- **请求体**: 
  ```json
  {
    "username": "testuser",
    "email": "test@example.com",
    "password": "test123"
  }
  ```

### 用户登录
- **URL**: `/login`
- **方法**: POST
- **请求体**: 
  ```json
  {
    "username": "testuser",
    "password": "test123"
  }
  ```

## 开发说明

### 代码规范

- 使用TypeScript编写所有组件
- 遵循React Hooks最佳实践
- 使用ESLint进行代码检查
- 使用Prettier进行代码格式化

### 添加新功能

1. 在`components/`目录下创建新组件
2. 在`api.ts`中添加API接口定义
3. 在`App.tsx`中配置路由
4. 添加适当的样式到`App.css`

### 测试

```bash
npm test
```

## 许可证

MIT

## 贡献

欢迎提交Issue和Pull Request！
